name: Lint

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  yaml:
    name: yamllint (docker-compose.yaml)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: pip install yamllint
      - run: yamllint docker-compose.yaml
